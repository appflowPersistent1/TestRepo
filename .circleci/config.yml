# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  say-hello:
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: cimg/base:stable
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  say-hello-workflow412:
    jobs:
      - say-hello
  say-hello-workflow413:
    jobs:
      - say-hello
  say-hello-workflow414:
    jobs:
      - say-hello
  say-hello-workflow415:
    jobs:
      - say-hello
  say-hello-workflow416:
    jobs:
      - say-hello
  say-hello-workflow417:
    jobs:
      - say-hello
  say-hello-workflow418:
    jobs:
      - say-hello
  say-hello-workflow419:
    jobs:
      - say-hello
  say-hello-workflow420:
    jobs:
      - say-hello
  say-hello-workflow421:
    jobs:
      - say-hello
  say-hello-workflow422:
    jobs:
      - say-hello
  say-hello-workflow423:
    jobs:
      - say-hello
  say-hello-workflow424:
    jobs:
      - say-hello
  say-hello-workflow425:
    jobs:
      - say-hello
  say-hello-workflow426:
    jobs:
      - say-hello
  say-hello-workflow427:
    jobs:
      - say-hello
  say-hello-workflow428:
    jobs:
      - say-hello
  say-hello-workflow429:
    jobs:
      - say-hello
  say-hello-workflow430:
    jobs:
      - say-hello
  say-hello-workflow431:
    jobs:
      - say-hello
  say-hello-workflow432:
    jobs:
      - say-hello
  say-hello-workflow433:
    jobs:
      - say-hello
  say-hello-workflow434:
    jobs:
      - say-hello
  say-hello-workflow435:
    jobs:
      - say-hello